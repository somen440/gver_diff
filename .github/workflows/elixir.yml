name: Elixir CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: circleci/elixir:1.9.4

    steps:
    - uses: actions/checkout@v1
    - name: Install Dependencies
      run: make build
    - name: Run Tests
      run: make test
